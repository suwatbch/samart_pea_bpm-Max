//----------------------------------------------------------------------------------------
// patterns & practices - Smart Client Software Factory - Guidance Package
//
// This file was generated by the "Add Foundational Module" recipe.
//
// The Module class derives from the CAB base class ModuleInit and it will be instantiated
// when the module is loaded by the CAB infrastructure.
//
// For more information see:
// ms-help://MS.VSCC.v80/MS.VSIPCC.v80/ms.scsf.2006jun/SCSF/html/03-270-Creating%20a%20Module.htm
//
// Latest version of this Guidance Package: http://go.microsoft.com/fwlink/?LinkId=62182
//----------------------------------------------------------------------------------------

using Microsoft.Practices.CompositeUI;
using Microsoft.Practices.CompositeUI.Services;
using Microsoft.Practices.ObjectBuilder;
using PEA.BPM.PaymentCollectionModule.Constants;
using PEA.BPM.Infrastructure.Interface;
using PEA.BPM.Architecture.CommonUtilities;
using PEA.BPM.PaymentCollectionModule.Interface.Constants;

namespace PEA.BPM.PaymentCollectionModule
{
    public class Module : ModuleInit
    {
        private WorkItem _rootWorkItem;

        [InjectionConstructor]
        public Module([ServiceDependency] WorkItem rootWorkItem)
        {
            _rootWorkItem = rootWorkItem;
        }

        public override void Load()
        {
            base.Load();
            ControlledWorkItem<ModuleController> wi = _rootWorkItem.WorkItems.AddNew<ControlledWorkItem<ModuleController>>();
            wi.Controller.Run();


            //SetLocalSetting();
        }

        private static void SetLocalSetting()
        {
            LocalSettingHelper hp = LocalSettingHelper.Instance();
            if (hp.Read(LocalSettingHelper.BranchId) == null)
            {
                hp.Add(LocalSettingHelper.BranchId, "B00001");
            }
            if (hp.Read(LocalSettingNames.BranchName) == null)
            {
                hp.Add(LocalSettingNames.BranchName, "°“√‰øøÈ“ Ë«π¿Ÿ¡‘¿“§®—ßÀ«—¥≈”æŸπ");
            }
            if (hp.Read("BranchAddress1") == null)
            {
                hp.Add("BranchAddress1", "137 ∂ππ≈”æŸπ - ªË“´“ß µ”∫≈„π‡¡◊Õß");
            }
            if (hp.Read("BranchAddress2") == null)
            {
                hp.Add("BranchAddress2", "Õ”‡¿Õ‡¡◊Õß≈”æŸπ ®—ßÀ«—¥≈”æŸπ");
            }
            if (hp.Read("TaxId") == null)
            {
                hp.Add("TaxId", "4102030569");
            }
            if (hp.Read("PosNo") == null)
            {
                hp.Add("PosNo", "54322");
            }
            if (hp.Read("PosId") == null)
            {
                hp.Add("PosId", "Ø 51010 91 00151");
            }
        }
    }
}
